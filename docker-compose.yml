services:
  backend:
    build:
      context: ./backend
    container_name: progressarr-backend
    env_file:
      - .env.local
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
