services:
  backend:
    build:
      context: ./backend
    container_name: progressarr-backend
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
